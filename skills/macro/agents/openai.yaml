interface:
  display_name: "Macroeconomic Data"
  short_description: "FRED economic observations and trends"
  default_prompt: "Use $macro to compare recent US inflation and unemployment."
dependencies:
  tools:
    - type: "mcp"
      value: "expert-system"
      description: "Expert System research, macroeconomic data, and financials"
      transport: "streamable_http"
      url: "https://expert-system.starmode.dev/api/mcp"
policy:
  allow_implicit_invocation: true
