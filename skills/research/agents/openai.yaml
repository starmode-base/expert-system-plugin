interface:
  display_name: "Research"
  short_description: "Curated research with source attribution"
  default_prompt: "Use $research to summarize recent AI infrastructure research."
dependencies:
  tools:
    - type: "mcp"
      value: "expert-system"
      description: "Expert System research, macroeconomic data, and financials"
      transport: "streamable_http"
      url: "https://expert-system.starmode.dev/api/mcp"
policy:
  allow_implicit_invocation: true
